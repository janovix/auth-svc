npx lint-staged
pnpm run lint
pnpm run format:check
pnpm run typecheck
node -e "if(process.platform==='win32'){console.log('⚠️  Skipping auth-svc coverage check on Windows (vitest-pool-workers module resolution issue)');process.exit(0)}else{process.exit(1)}" || pnpm run vitest:coverage
